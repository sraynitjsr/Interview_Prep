package main

import "fmt"

func main() {

	input := []int{0, 4, 3, 2, 7, 8, 2, 3, 1}
	myMap := make(map[int]int)
	for _, d := range input {
		val, present := myMap[d]
		if present {
			myMap[d] = val + 1
		} else {
			myMap[d] = 1
		}
	}
	for k, v := range myMap {
		if v > 1 {
			fmt.Println(k)
		}
	}
}

